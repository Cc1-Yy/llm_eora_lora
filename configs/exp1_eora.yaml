model_name: "gpt2"
task_type: "classification"
num_labels: 2

seed: 42
output_dir: "outputs/exp1_eora"

# TODO: 关键输入，根据任务修改
optimized_model_dir: "outputs/optimized_minimal/model"
quantized_model_dir: "outputs/optimized_gptq_4bit"

data:
  dataset_name: "glue/sst2"
  dataset_config_name: null
  max_length: 128
  batch_size: 8
  num_workers: 0
  use_internal_test: true
  internal_test_size: 0.1


eora:
  rank: 8
  calibration_num_samples: 512
