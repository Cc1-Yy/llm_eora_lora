model_name: "gpt2"
task_type: "lm"
seed: 42
output_dir: "outputs/optimized_lm_1m"

data:
  train_corpus:
    name: "allenai/c4"
    config: "en"
    split: "train"
    streaming: true
    max_train_samples: 1000000
    text_key: "text"
    shuffle_buffer: 20000

  eval_corpus:
    local_disk_path: "data_cache/wikitext2_raw"
    split: "validation"
    text_key: "text"

  test_corpus:
    local_disk_path: "data_cache/wikitext2_raw"
    split: "test.py"
    text_key: "text"

  max_length: 1024
  batch_size: 2
  num_workers: 0

train:
  lr: 2e-5
  weight_decay: 0.01
  num_epochs: 1
  grad_clip: 1.0
