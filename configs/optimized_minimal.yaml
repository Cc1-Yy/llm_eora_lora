model_name: "gpt2"
task_type: "classification"
num_labels: 2

data:
  dataset_name: "glue/sst2"
  dataset_config_name: null
  max_length: 128
  batch_size: 8
  num_workers: 0

train:
  lr: 5e-5
  weight_decay: 0.01
  num_epochs: 3
  grad_clip: 1.0
  eval_every_steps: 0   # 0 表示按 epoch 验证

output_dir: "outputs/optimized_minimal"
seed: 42
